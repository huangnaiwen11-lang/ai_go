# 候选淘汰清单

本清单只记录审计证据，不执行删除。条目必须同时核对前端/后台调用、运行时路由、配置开关、测试契约和生产观测；负责人确认后才能转为“已确认废弃”。

## 当前候选

- `api/todo/v1` Todo 示例：仅作为 Kratos 模板参考，未发现其属于主站业务迁移范围。
- Go 模板自带的示例流式接口和 OpenAPI 示例：未与主站 `/api` 契约建立映射。

## 删除门禁

在没有负责人确认、数据影响评估和可恢复方案前，不删除任何 Node、React 或 Go 文件。
